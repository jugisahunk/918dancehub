PUBLIC_FORMSPREE_FORM_ID=

# Cloudflare Web Analytics beacon token (issue #18). Only renders on
# production builds (detected via Cloudflare Pages' own CF_PAGES env var) —
# leave unset locally and in GitHub Pages staging.
PUBLIC_CF_BEACON_TOKEN=
